# Caravel / OpenLANE support for SCOoOTER

First, launch the `setup-caravel.py` script to initially configure the caravel repo. Use the `--rootless` option if using rootless Docker. Afterwards, use the `build_and_run_asic.py` script in the toplevel repo.